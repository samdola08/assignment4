 What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?


=> getElementById : Its worked on single element, its can't reusable, It will be unique.
=> getElementsByClassName : Its apply on every class.
=> querySelector : This use on single and first CSS selector.
=> querySelectorAll : This applied on every CSS element.


How do you create and insert a new element into the DOM?

=> create a element in html file, then give them a class id, then add append, it will work in dom.

What is Event Bubbling? And how does it work?

=> Event Bubbling is a event propagation system. If click a button it will start will child element then one step up go to parant element.

What is Event Delegation in JavaScript? Why is it useful?

=> Event Delegation is a parent listener, its work also child element even if you can't touch the child element. Its dynamic content-friendly.

What is the difference between preventDefault() and stopPropagation()?

=>preventDefault() This disables the default behavior of the event.
stopPropagation() This disables the event inner element to parent elements